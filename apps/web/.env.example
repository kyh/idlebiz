# Stripe Connect platform credentials (see src/lib/env.ts).
# Both optional: without them /api/stripe/* refuses the flow and the landing
# page is unaffected. Set on Vercel for the real deploy.
STRIPE_CLIENT_ID=
STRIPE_SECRET_KEY=
