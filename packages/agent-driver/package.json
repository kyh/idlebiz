{
  "name": "@repo/agent-driver",
  "version": "0.1.0",
  "private": true,
  "description": "CLI coding-agent runners (claude, codex) over the Agent Client Protocol: spawn, run one turn, price usage",
  "type": "module",
  "exports": {
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf .cache .turbo"
  },
  "dependencies": {
    "@agentclientprotocol/sdk": "^1.4.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "catalog:"
  }
}
